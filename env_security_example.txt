# Security Configuration Environment Variables

# Core Application Settings
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_ADMIN_ID=your_admin_telegram_user_id

# AI Provider Configuration
AI_PROVIDER=gemini              # Options: 'gemini' or 'openai'

# Gemini AI Settings
GEMINI_API_KEY=your_gemini_api_key_here

# OpenAI Settings (only needed if AI_PROVIDER=openai)
OPENAI_API_KEY=your_openai_api_key_here

# Security Limits
MAX_FILE_SIZE=10485760          # Maximum file size in bytes (10MB default)
MAX_USERS=100                   # Maximum number of users allowed
RATE_LIMIT_REQUESTS=10          # Number of requests per time window
RATE_LIMIT_WINDOW=60            # Rate limit time window in seconds

# Logging Configuration
LOG_LEVEL=INFO                  # DEBUG, INFO, WARNING, ERROR, CRITICAL

# Cloud Configuration
USE_WEBHOOK=true                # Use webhooks for production (set to false for polling)
WEBHOOK_URL=your_webhook_url    # Optional: Override auto-detected webhook URL
PORT=8080                       # Port for webhook server

# Google Cloud Configuration (Optional - use service account instead)
# GOOGLE_APPLICATION_CREDENTIALS=path_to_service_account_key.json
GOOGLE_CLOUD_PROJECT=your_project_id

# Optional Security Settings
SESSION_TIMEOUT_HOURS=24        # Session timeout in hours
CLEANUP_INTERVAL_MINUTES=60     # How often to run cleanup tasks
BACKUP_INTERVAL_MINUTES=10      # How often to backup database